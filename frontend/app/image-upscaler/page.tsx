import { ImageUpscalerStudio } from '@/components/image-upscaler-studio'

export const metadata = {
  title: 'Image Upscaler — Photo Finale',
  description: 'Restore low-quality photos and upscale them to crisp, print-ready resolution.',
}

export default function ImageUpscalerPage() {
  return <ImageUpscalerStudio />
}
