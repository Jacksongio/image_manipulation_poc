import { BorderExpanderStudio } from '@/components/border-expander-studio'

export const metadata = {
  title: 'Border Expander — Photo Finale',
  description: 'Extend photo backgrounds naturally to fit 4 × 6 and 5 × 7 print sizes without cropping.',
}

export default function BorderExpanderPage() {
  return <BorderExpanderStudio />
}
